# Claude Code Instructions for Zenn Writing Environment

このリポジトリは **Zenn テックブログ記事** を執筆するための環境です。記事の執筆・編集・レビューを行う際は、必ず `writing-guideline/guideline.md` に記載されたガイドラインに準拠してください。

## Submodule 運用ルール

- `writing-guideline` は Git submodule のため、直接編集しない
- ガイドラインの変更が必要な場合は、submodule 側のリポジトリで変更し、このリポジトリでは submodule の参照更新のみ行う

## 基本方針

### 文体とトーン
- **ですます調** を使用する
- 読者に語りかけるような親しみやすいトーンを心がける
- くだけすぎない程度に丁寧さを保つ

### 想定読者
- 同業のエンジニア（ジュニア〜シニア）
- テーマに特有の専門知識は前提としない
- 必要に応じて前提知識や参考記事へのリンクを冒頭に記載する

## 記事作成時の重要ルール

### 1. 正確性（最優先事項）
- **公式ドキュメントの内容と矛盾しない** 正確な技術情報を記載する
- コードサンプルは **必ず動作確認** を行ってから掲載する
- 使用しているライブラリ・フレームワークの **バージョンを明記** する
- 引用元・参考文献は正しくリンクする
- 非推奨（deprecated）な API やパターンは使用しない

### 2. 構成と見出し
- 本文中の見出しは `##`（h2）から始める（`#` は Zenn が自動生成）
- 見出しの階層は飛ばさない（`##` の次は `###`）
- 見出しだけを読んで記事の流れが把握できるようにする
- 記事タイトルには具体的な技術名やバージョンを含める
  - 例: 「Next.js 15 の Server Actions で CRUD API を実装する」

### 3. 文章の書き方
- 1文は **60〜80 文字程度** を目安にする
- 1つの段落では1つのトピックを扱う
- 3文程度連続したら段落を区切る
- 接続詞を活用して文のつながりを明確にする

### 4. 用語・表記の統一
- 技術用語は **公式ドキュメントに準拠** した表記を使用する
  - 例: `GitHub`（○）/ `Github`（×）
  - 例: `TypeScript`（○）/ `Typescript`（×）
  - 例: `Next.js`（○）/ `NextJS`（×）
- カタカナ語の長音符（ー）は原則として JIS 規格に従う
  - 例: サーバー（○）/ サーバ（×）
  - 例: ユーザー（○）/ ユーザ（×）
- 英数字は **半角** を使用する
- 括弧は和文中では **全角**、コードやコマンド中では **半角** を使用する
- 句点は「。」、読点は「、」を使用する（カンマ・ピリオドは使用しない）

### 5. 再現性
- 手順記事の場合、手順通りに実行して動作することを確認する
- 実行環境（OS、言語バージョン、主要ライブラリのバージョン）を明記する
- 前提条件（必要なツールのインストール、アカウント作成など）を記載する
- サンプルコードは部分的でなく、読者が再現できる十分な情報を含める

## Zenn 固有の記法を積極的に活用

### メッセージブロック
補足情報や注意書きには、メッセージブロックを使用します。

```markdown
:::message
補足情報をここに記載します。
:::

:::message alert
注意事項や警告をここに記載します。
:::
```

### アコーディオン（トグル）
長いコードや補足的な説明は、アコーディオンで折りたたみます。

```markdown
:::details タイトル
折りたたまれる内容をここに記載します。
:::
```

### コードブロック
- **言語指定を必ず付ける**（シンタックスハイライトのため）
- ファイル名の指定も活用する

````markdown
```typescript:src/app/api/route.ts
export async function GET() {
  return Response.json({ message: "Hello" });
}
```
````

### diff 表示
変更前後を示す場合は diff 記法を使います。

````markdown
```diff
- const oldValue = "before";
+ const newValue = "after";
```
````

### 画像
- 画像には **alt テキストを必ず設定** する
- スクリーンショットは必要最小限の範囲をトリミングする
- 画像だけで情報を伝えず、本文中でも説明する

### リンク
- 外部リンクにはリンク先の内容がわかるテキストを設定する
  - 例: [Next.js 公式ドキュメント](https://nextjs.org/docs)（○）
  - 例: [こちら](https://nextjs.org/docs)（×）
- 公式ドキュメントへのリンクは積極的に貼る

## Front Matter の設定

記事の先頭には以下の Front Matter を設定してください。

```yaml
---
title: "記事のタイトル（70文字以内推奨）"
emoji: "📝"
type: "tech" # tech: 技術記事 / idea: アイデア記事
topics: ["nextjs", "typescript", "react"] # 最大5つ
published: false # 公開状態
---
```

## レビュー・フィードバック時の対応

記事のレビューやフィードバックを提供する際は、以下のプレフィックスを使用して重要度を明確にします。

| プレフィックス | 意味 | 例 |
|---|---|---|
| `[must]` | 必ず修正が必要 | 技術的な誤り、コードの不具合 |
| `[nits]` | 些細な指摘・好みの範囲 | 表現の微調整、表記揺れ |
| `[q]` | 質問・確認事項 | 意図の確認、補足情報の要求 |

## 記事作成・編集ワークフロー

1. **記事の作成**: `npx zenn new:article` で新規記事を作成
2. **執筆**: 上記ガイドラインに従って執筆
3. **動作確認**: コードサンプルは必ず動作確認を行う
4. **プレビュー**: `npx zenn preview` でローカルプレビューを確認
5. **AIレビュー**: `/review-article articles/記事名.md` でレビューを実行（推奨）
6. **修正**: レビュー結果を踏まえて修正
7. **公開**: Front Matter の `published` を `true` に変更

## 記事レビュー（AI統合版）

`/review-article` の運用手順・出力形式は `review-article-core.md` を参照する。

```bash
/review-article articles/記事名.md
```

## 参考資料

- 詳細なガイドライン: [writing-guideline/guideline.md](writing-guideline/guideline.md)
- PRレビューガイドライン: [writing-guideline/pr-review-guideline.md](writing-guideline/pr-review-guideline.md)
- Zenn公式ドキュメント: https://zenn.dev/zenn/articles/markdown-guide

## 注意事項

- 記事を編集する際は、既存の内容を尊重しつつ、ガイドラインに沿った改善を提案する
- 技術的な正確性が最優先。不確かな情報は公式ドキュメントで確認する
- コードサンプルは必ず動作確認済みのものを掲載する
- 読者目線で「わかりやすさ」を常に意識する
